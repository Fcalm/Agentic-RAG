# SuperMew（喵喵助手）AGENT-RAG 项目经历包装

> 用途：AI Agent 产品经理求职，将本项目转化为简历项目经历。
> 定位一句话：**一个"可解释、可控成本、可人工介入"的企业级知识库 Agent 产品**（LangChain Agent + 自研 RAG 链路 + 现代化前端）。

---

## 一、架构拆解（技术地图）

### 技术栈

| 层级   | 选型                                                                                |
| ---- | --------------------------------------------------------------------------------- |
| 前端   | Vite + Vue 3 + TypeScript + Pinia + Axios + Sass（工程化 SFC 架构，后端静态托管 dist）          |
| 后端   | FastAPI + LangChain Agent + **LangGraph 状态机** + Pydantic + SQLAlchemy             |
| 向量检索 | Milvus 2.5+（HNSW 稠密索引 + SPARSE_INVERTED_INDEX 稀疏索引）、RRF 融合、Jina Rerank 精排         |
| 嵌入   | 本地 `BAAI/bge-m3`（1024 维稠密）+ Milvus **原生 BM25**（FunctionType.BM25 + chinese 分词器）稀疏 |
| 数据层  | PostgreSQL（会话/父块/用户）+ Redis（热缓存）+ Milvus（叶子向量）                                    |
| 鉴权   | JWT + RBAC（admin/user 角色隔离），PBKDF2-SHA256（310000 轮）                               |

### 分层架构

```
┌────────────────────────────────────────────────────────────┐
│ 前端 (Vite+Vue3+TS+Pinia)                                   │
│  Thinking 状态机 · SSE 流式解包 · AbortController 终止       │
└───────────────┬────────────────────────────────────────────┘
                │ POST /chat/stream  (text/event-stream)
┌───────────────▼────────────────────────────────────────────┐
│ FastAPI API 层  →  auth / sessions / chat / documents       │
├─────────────────────────────────────────────────────────────┤
│ 对话域 chat/service.py：                                  │
│   Request Context + Threadsafe Callback（跨线程事件推送）    │
│   agent.astream 逐 token · asyncio.Queue · 会话摘要记忆     │
├─────────────────────────────────────────────────────────────┤
│ RAG 域 rag/pipeline.py（LangGraph 状态机）                   │
│  复杂度路由 → 并行子Agent(Send) / 标准检索                   │
│  → 证据评分(GRADE_MODEL) → 路由 → Step-back/HyDE 单选重写    │
│  → 二次召回 → Synthesis 去重合并 · HITL 断点与恢复           │
├─────────────────────────────────────────────────────────────┤
│ 索引域 indexing/：三级滑动窗口分块 → Leaf-only 向量化        │
│  Milvus 原生 BM25 + bge-m3 双塔 · 父块入库 PostgreSQL        │
├─────────────────────────────────────────────────────────────┤
│ 基础设施：PostgreSQL / Redis / Milvus(etcd+minio+attu)      │
└─────────────────────────────────────────────────────────────┘
```

### 三大核心链路

1. **RAG 全链路**：复杂度规划 → 检索（Hybrid→Auto-merging→Rerank）→ 证据评判门控 → 单选重写 → 二次召回 → 合成回答。
2. **文档入库链路**：上传 → 5 阶段异步任务（上传/清理旧版/解析分块/父块入库/向量化）→ 同名重复上传自动清旧，保证库内一致。
3. **流式交互链路**：同步工具在线程池执行时，通过 `loop.call_soon_threadsafe` 把 RAG 步骤实时推回主事件循环 → SSE → 前端"思考→回答"同气泡无缝过渡。

---

## 二、四个关键点（开发视角 × 业务视角）

> 选点逻辑：贴合 **AI Agent 产品经理** 岗位，兼顾工程深度与产品商业价值（成本、体验、信任、幻觉、一致性）。

### 关键点 1｜低延迟复杂度规划 + 并行 Sub-Agent 检索

**开发视角**

- 用 LangGraph 状态机构建 `classify_complexity → (retrieve_initial | prepare_sub_questions) → grade → rewrite → synthesis` 工作流。
- 明显单事实问题（本地规则匹配"是什么/谁/多少"等标记、短句）**零模型调用短路**为 simple。
- FAST_MODEL **一次结构化调用**同时输出 simple/complex 判断 + 复杂问题的 2-4 个子问题（避免二次拆题调用）。
- 复杂问题经 LangGraph `Send` API **并行**执行各子问题"检索→证据评判"，Synthesis 节点按 chunk_id 去重合并。

**业务视角**

- 回答延迟与单次问答成本是 RAG 产品的核心北极星。简单问题走最短路（省成本、降首字延迟）；复杂问题并行检索摊薄最坏延迟。
- 将"拆题"从 2 次模型调用压缩为 1 次，是架构级的**成本杠杆**。

### 关键点 2｜纠错型 RAG（Corrective RAG）+ 证据路由 + HITL

**开发视角**

- 独立 GRADE_MODEL 用结构化输出一次性评判：`relevance / answerability / ambiguity / route / confidence`。
- 路由仅允许 5 态：answer / rewrite / clarify / scope_select / no_knowledge；空检索直接短路，不硬答。
- 证据不足时 FAST_MODEL 在 **Step-back / HyDE 单选一种**，重写检索**预算硬限 1 次**（`rewrite_count < 1`）。
- clarify / scope_select 触发 **HITL 断点**，用户补充后跳过复杂度判断直接针对性检索恢复（`resume_rag_from_hitl`）。

**业务视角**

- 直接对"**AI 幻觉**"与"**答非所问**"两个信任杀手做产品化拦截：**证据不足不硬答**，转为"主动澄清/给选项"的可交互流程，并保持任务不中断（可恢复）。
- 重写预算=1 次，将"最坏延迟"与"token 成本"钉死在可控上界——这是可对用户/老板承诺的 SLA 级设计。

### 关键点 3｜三级分块 + Milvus 原生混合检索（召回质量）

**开发视角**

- 三级滑动窗口分块：L1 2400 / L2 1600 / L3 800 字符，overlap 400/200/100；**Leaf-only** 策略——仅 L3 叶子入 Milvus，L1/L2 父块入 PostgreSQL DocStore。
- Milvus 2.5+ **原生 BM25**（`FunctionType.BM25` + chinese 分词器，稀疏向量统计由向量库服务端自动维护，对齐零误差），与 bge-m3 稠密向量双路 `AnnSearchRequest` + `RRFRanker(k=60)` 融合。
- Jina Rerank 精排：30 候选（RETRIEVAL_CANDIDATE_K=30）→ Auto-merging（≥2 子块合并升父级）→ 精排 top 8；Hybrid 失败自动降级纯稠密。

**业务视角**

- 召回质量 = 回答准确率的上限。混合检索兼顾**语义 + 关键词**（产品名/型号/实体词），Rerank 把最相关片段顶进 top 8 喂给模型，Auto-merging 保住跨段落上下文。
- 原生 BM25 把分词统计的工程维护成本转移给向量库——**技术选型直接兑现"答得准"的用户价值与低维护成本**。

### 关键点 4｜实时可观测 RAG + 流式交互体验

**开发视角**

- 解决"同步工具阻塞 asyncio 事件循环 → 思考过程不可见"的痛点：**Request Context + Threadsafe Callback** 模式，每请求捕获主 loop，RAG 节点用 `loop.call_soon_threadsafe(queue.put_nowait)` 跨线程投递 `rag_step` 事件。
- 后端 `agent.astream(stream_mode="messages")` 逐 token + SSE；前端 `Thinking 状态机`（Idle→Thinking→Streaming），**同一气泡**从"思考→检索→回答"无缝过渡。
- 回答可中断：前端 `AbortController` + 后端显式捕获 `GeneratorExit → agent_task.cancel()`，确定性停止推理、即时止损 token。

**业务视角**

- 大模型"静默思考"是用户焦虑与流失点；把检索→评分→重写→合并全过程可视化，**黑盒变白盒，建立信任**。
- 一键中断 = 直接节省 token 成本；思考步骤带 `elapsed_ms` 计时，为后续延迟优化提供观测数据。

---

## 三、简历版项目经历（每条约 35-50 字，保留专业术语）

> 结构：**总结：具体实现方式 + 结果**；数字均为架构级硬指标，可直接用于简历与面试（替换建议见第四节）。
> 字数口径：中文字符计 1，连续英文/数字术语计 1（如 LangGraph、BM25、2-4），标点不计。

**1.（Agent 编排 / 成本延迟）**

> 总结：LangGraph按复杂度分流，简单问题本地规则短路免拆题调用，复杂问题拆2-4子问题并行检索，控延迟降成本。

**2.（纠错型 RAG / 幻觉与体验）**

> 总结：检索后GRADE_MODEL结构化评判证据，不足时Step-back/HyDE单选重写预算1次，歧义触发HITL澄清续跑，控幻觉降成本。

**3.（混合检索 / 召回质量）**

> 总结：Milvus原生BM25+稠密双路混合检索经RRF融合Rerank精排30选8，三级分块Auto-merging，重复上传自动清理旧数据。

**4.（可观测 / 用户体验）**

> 总结：跨线程事件调度实时推送RAG思考步骤，SSE逐token流式输出，前端Thinking状态机同气泡无缝过渡，回答可中断。

| #   | 字数  | 主视角   | 量化点                                 |
| --- | --- | ----- | ----------------------------------- |
| 1   | 43  | 成本/延迟 | 2-4 子问题并行、简单问题免拆题调用                 |
| 2   | 41  | 幻觉/体验 | GRADE_MODEL 结构化评判、重写预算 1 次、HITL 可恢复 |
| 3   | 40  | 召回质量  | 原生 BM25、RRF k=60、精排 30→8、三级分块       |
| 4   | 43  | 体验/成本 | 逐 token 流式、状态机同气泡、可中断省 token        |

### 精简备选（20-35 字版，适合字符受限的投递渠道）

**1.** 总结：LangGraph按复杂度分流，复杂问题拆2-4子问题并行检索，简单问题本地短路控成本。（33字）
**2.** 总结：证据不足时Step-back/HyDE单选重写预算1次，歧义走HITL澄清续跑，控幻觉降成本。（29字）
**3.** 总结：Milvus原生BM25+稠密混合检索经RRF融合Rerank精排8条，三级分块Auto-merging，重复上传清理旧数据。（33字）
**4.** 总结：跨线程调度实时推送RAG思考步骤，SSE逐token流式输出，思考回答同气泡过渡且可中断。（34字）

---

## 四、结果数据的可信度与升级建议

**当前数字均为"架构级硬指标"**（代码与配置可直接佐证，面试可讲得实），不是编造的评测成绩：

- 可信硬指标：2-4 子问题 / 重写预算 1 次 / 候选 30 精排 8 / RRF k=60 / 三级分块 800-1600-2400 / 每次问答知识库工具至多调用 1 次 / JWT 1440 分钟 / PBKDF2 310000 轮。

**若要升级为"效果级数据"（推荐，面试杀伤力更大）**：项目已内置 `langsmith_eval.py`（LangSmith 评测 + RAG 数据集），README 的 Todo 也规划了"小型标注集对比 dense-only / sparse-only / hybrid / hybrid+rerank"。跑通后可将第 3 条升级为：

> 总结：搭建Dense+BM25混合检索与Rerank精排流水线，按标注集评测gold chunk命中率，相对纯稠密提升X%。

同理第 1 条可补"首 token 延迟降低 X%"，第 2 条可补"问答准确率提升 X%、幻觉率下降 X%"。**未测出的数字不要先写。**

---

## 五、面试表达提示（产品化话术）

每个关键点用"**痛点 → 方案 → 量化结果 → 商业价值**"四段式讲，更贴合 PM 视角：

1. **成本/延迟**：一句话点出"LLM 每次调用都是成本"，用"复杂度分流 + 并行 + 短路"三招控制，最后落到"成本-延迟可承诺上界"。
2. **幻觉**：强调"AI 幻觉是产品信任的致命伤"，用"证据门控 + 不硬答 + 主动澄清 + 可恢复 HITL"做产品化兜底——这是 Agent PM 最该讲的话题。
3. **召回质量**：用"召回决定准确率上限"切入，讲清语义+关键词互补、精排保 top 8、自动合并保上下文，结尾点"原生 BM25 把维护成本还给向量库"。
4. **体验/可观测**：用"静默思考=焦虑=流失"切入，讲"过程白盒化 + 打字机流式 + 可中断省 token"，突出你对**体验指标与成本**的双敏感。

**通用加分句**（面试可引）：

- "我把每次问答的模型调用次数和 token 上界都做成了架构约束，而不是事后优化。"
- "产品层面我把'答不出来'设计成'主动澄清/给选项'的交互，而不是让用户面对一个错误答案。"
